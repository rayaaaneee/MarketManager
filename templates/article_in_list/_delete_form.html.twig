<form method="post" action="{{ path('app_article_in_list_delete', {'id': article_in_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_in_list.id) }}">
    <button class="btn">Delete</button>
</form>
