<form method="post" action="{{ path('app_collaboration_request_delete', {'id': collaboration_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaboration_request.id) }}">
    <button class="btn">Delete</button>
</form>
