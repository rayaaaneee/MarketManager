

{% extends 'base.html.twig' %}

{% block title %}{{ article.Name }} - Article{% endblock %}

{% block body %}

{% block stylesheet %}
<style>
  .container {
    display: flex;
    align-items: center;
    margin-left : 20px;
    margin-top : 10px;
  }

  .container img {
    width: 150px;
    height: 150px;
    margin-right: 20px;
  }
</style>
{% endblock %}


<div class = "container">
  <img src="{{ asset('img/article/' ~ article.image) }}" alt="{{ article.name }}" draggable="false">
  <div class="text-container">
    <h1>{{ article.Name }}</h1>
    <h3>Unity Price: {{ article.UnityPrice }} €</h3>
    <h3>Type: {{ article.Type.name }}</h3>
  </div>
</div>    

<div class = "form">
    <p>{{articleInList.name}}</p>
    <p>{{articleInList.brand}}</p>
    <p>{{articleInList.quantity}}</p>
</div>
  
{{ include ('footer.html.twig') }}
{%endblock%}


