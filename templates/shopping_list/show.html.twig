{% extends 'base.html.twig' %}

{% block title %}ShoppingList{% endblock %}

{% block body %}
    <h1>ShoppingList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shopping_list.id }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ shopping_list.idUser }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shopping_list.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shopping_list.description }}</td>
            </tr>
            <tr>
                <th>TotalPrice</th>
                <td>{{ shopping_list.totalPrice }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ shopping_list.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shopping_list_index') }}">back to list</a>

    <a href="{{ path('app_shopping_list_edit', {'id': shopping_list.id}) }}">edit</a>

    {{ include('shopping_list/_delete_form.html.twig') }}
{% endblock %}
